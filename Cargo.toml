[package]
name = "shadowbox"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.0", features = ["derive"] }
directories = "5.0.1"
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"
glob = "0.3"

[dev-dependencies]
tempfile = "3.27.0"
walkdir = "2.5"
